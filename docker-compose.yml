version: "2"
services:
  web:
    build:
      context: .
      dockerfile: webapp.dockerfile
    depends_on:
      - db
  db:
    build:
      context: .
      dockerfile: mongodb.dockerfile
    volumes:
      - /srv/gimmejson/db:/data/db
      - /srv/gimmejson/log:/var/log/mongod.log
